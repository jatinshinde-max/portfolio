{
  "name": "portfolio",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.js --bundle --outfile=dist/main.js --format=esm --sourcemap --watch",
    "build": "esbuild src/main.js --bundle --outfile=dist/main.js --format=esm --minify",
    "serve": "npx serve ."
  },
  "dependencies": {
    "@barba/core": "^2.10.3",
    "gsap": "^3.12.5",
    "three": "^0.176.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
